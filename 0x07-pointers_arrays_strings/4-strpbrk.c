#include "main.h"
#include <stdio.h>

#include <string.h>
/**
 * _strpbrk - check the code
 * @s: target location
 * @accept: value
 * Return: charecter.
 */
char *_strpbrk(char *s, char *accept)
{
return (strpbrk(s, accept));
}
